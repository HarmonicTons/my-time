import * as React from "react";
import AppLayout from "../business/AppLayout";

const Report = () => {
  return (
    <AppLayout
      pageName="report"
      pageContent={(() => (
        <>report</>
      ))()}
    />
  );
};

export default Report;
